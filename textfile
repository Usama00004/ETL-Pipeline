instance name = redfin-endyoutube-ym-EC2 = ETL_project_instance
OS = Ubuntu
instance type = recommended is t2 large but I am using micro because of credits


SSH connection
chmod 0400 {KEYNAME}
ssh -i {KEYNAME}.pem {USERNAME}@{IP}



1. sudo apt update 
fetches the latest version of this list from the repositories, 
ensuring your system knows about the most recent updates and available software.

2. sudo apt install python3-pip

3. (sudo apt install python3.10-venv)
   I am using sudo apt install python3.12-venv

4. python3 -m venv redfin_venv
   source redfin_venv/bin/activate

5. pip install pandas

6. pip install boto3

7. pip install --upgrade awscli

8. pip install apache-airflow

9. airflow version 



10. configure access key
    

11. aws configure

12. add AWS Access Key ID 
        Secret Access Key 
        region name 
        output format (enter)

13. Initialize Airflow
      airflow standalone